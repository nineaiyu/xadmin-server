# 后台页面 UI 一致性重构计划（2026.09）

- 状态：**已交付**（批次一~五全部完成，P0–P2 共 12 项收口；并于 2026-09-25 做过一轮全站复核与查漏补缺，见 §九）
- 日期：2026-09-24（批次四~五与复核轮于 2026-09-25 交付）
- 关联：[ADR-056](../adr/ADR-056-data-permission-config-and-scope.md)（数据权限配置页重构与生效范围归一，本计划的规范样板）、
  [后台页面审计整改计划-2026.09.md](archive/后台页面审计整改计划-2026.09.md)（缺陷整改口径，已归档）、
  [长期优化方案-2034.10-2039.09.md](长期优化方案-2034.10-2039.09.md)（用户界面维度）。

## 一、背景

数据权限配置页重构（ADR-056）沉淀出一套可复用的页面规范：列表页一律 `RePlusPage`、新增/编辑走抽屉或统一弹层、
规则类复杂配置用「行内编辑卡片 + 可读摘要」、表单值以中文标签而非法名呈现。

随后对全站页面做了一次一致性评估，发现**同类页面存在两种做法**：已重构页面高度统一，而少数页面仍是
「手写表格 + 手写弹窗 + 内联表单 + 页面级样式」的旧形态；另有若干页面在**颜色/字号**上写死具体值，
主题与暗色模式下不跟随。本计划记录评估结论、重构清单、优先级与批次排期，作为后续实施的依据。

## 二、评估口径

### 2.1 基准规范（判断依据）

| 维度 | 基准 |
|------|------|
| 列表页 | `RePlusPage`（搜索区 / 表格 / 分页 / 列设置 / 行操作收敛一体化），页面侧只提供 `api`、`auth`、`addOrEditOptions`、`listColumnsFormat`、`operationButtonsProps` |
| 弹层 | 新增/编辑用 `addDialog`（ReDialog）或 `addDrawer`（ReDrawer）；**实体编辑禁止手写 `el-dialog` 外壳** |
| 表单 | 独立 `components/XxxForm.vue` + `getPayload()`（返回 `null` 表示保持弹窗） |
| 样式 | UnoCSS 原子类 + `var(--el-*)` 变量；页面级 `<style>` 尽量为空，不写死颜色 / 字号 / 圆角 |
| i18n | 全量 `t()`，zh / en 词条对称 |
| 行操作 | ≤5 个按钮，超出用「更多」收敛；复杂实体行操作走「管理」抽屉（`ReActionPanel` 模式） |

### 2.2 合理例外（不纳入重构）

1. **可视化自绘页**：看板（`dashboard`）、大屏投屏（`analysis/screen/display.vue`）、工作台图表、聊天室（`chat`）、
   AI 助手主界面（`integration/ai`）——非列表范式，栅格 / 画布 / 气泡布局是必然形态。
2. **只读展示层**：数据集执行预览、API 应用一次性密钥明文、`ScopeSelect` 这类**控件内选择弹窗**、
   各类预览抽屉（权限预览 / 试算结果 / 调用日志 / 访问记录 / 审批日志）。
3. **表单内子表**：审批节点编辑器、路由编辑、规则编辑、动态表单字段表、填报明细表等——它们是表单控件而非列表页。
4. **设置类页面**：`settings/basic`、`settings/ldap` 走 `SettingItem` + 后端 `search-columns` 通路，字段标签由服务端下发。

## 三、量化概览（2026-09-24 扫描）

| 指标 | 结果 |
|------|------|
| 页面入口（`index.vue`） | 58 个（另有 `analysis/screen/display.vue`、`oauth/authorize.vue` 两个附属页） |
| 使用 `RePlusPage` 的入口 | 37 个 |
| 页面级无 `<style>` 块的入口 | 47 / 58（81%） |
| 页面入口内手写表格 | 4 处：`form/my`、`system/credential`、`system/module`、`dashboard/dataset`（最后一处为允许的只读预览） |
| 手写 `el-dialog` | 4 处，其中仅 `settings/message` 的模板编辑弹窗为真问题，其余 3 处属允许场景 |
| 含 `el-table` 的文件 | 33 个，其中 29 个是表单子表 / 只读明细 / 监控面板（允许形态） |
| 模板内硬编码中文 | 1 处（`analysis/screen/display.vue` 的「远程控制中」）+ demo 示例建议词 |

**复核（2026-09-25，批次一~五交付后再扫一遍）**：

| 指标 | 结果 |
|------|------|
| 页面入口内手写表格 | 1 处（`dashboard/dataset` 只读预览，§2.2 允许） |
| 手写 `el-dialog` | 3 处，全部为 §2.2 允许场景（密钥明文 / 执行预览 / 控件内选择） |
| 含 `el-table` 的文件 | 23 个，全部为表单子表 / 只读明细 / 预览面板（允许形态） |
| 页面入口 `<style>` 超 12 行 | 6 个：`account`（定向侧栏皮肤）、`about`（EP 覆盖式样式）、`system/menu`（自绘树）、`welcome`（EP 卡片微调）、`login` / `empty`（独立页） |
| 写死字号 / 圆角（页面与公共组件） | **0 处**（本轮补齐 39 处字号 + 1 处圆角，见 §9.2） |
| 写死颜色（页面与公共组件） | 0 处（剩余为框架基础组件与色板常量，见 §9.3） |
| 模板内硬编码中文 | 0 处（登录页语言名「简体中文」是语言自称，按惯例保留） |
| 表单契约 | 25 个表单组件统一 `getPayload()`（本轮改名 2 处，见 §9.2） |
| 暗色模式验证 | 由「缺」补齐为 `e2e/theme-dark.e2e.ts`（见 §9.2） |

## 四、逐页判定

判定口径：**达标**（符合基准）/ **P0–P2**（需重构，见第五节）/ **例外**（2.2 的合理例外）/ **基准**（本轮样板块）。

### 4.1 系统管理域（33 页）

| 页面 | 实现形态 | 判定 |
|------|----------|------|
| `system/user` | RePlusPage + 行操作抽屉（UserActionPanel） | 达标（面板 scss 165 行 → P2） |
| `system/role` | RePlusPage + RoleForm + 授权树抽屉 | 达标（授权树 scss 60+60 行 → P2） |
| `system/dept` | RePlusPage + 预览抽屉 | 达标 |
| `system/menu` | 自绘树 + 页面级 scss（TreeRow/Toolbar/ContextMenu 约 237 行，写死 32px/14px/4px） | P1 |
| `system/dict` | RePlusPage + addDialog | 达标 |
| `system/field` | RePlusPage（只读） | 达标 |
| `system/file` | RePlusPage + 只读抽屉 | 达标 |
| `system/mask` | RePlusPage + addDialog | 达标 |
| `system/tag` | RePlusPage + TagForm | 达标（色板写死 → P1） |
| `system/notice` / `notice/read` | RePlusPage（+ addDialog） | 达标 |
| `system/online` | RePlusPage | 达标 |
| `system/module` | 手写卡片 + el-table + `text-gray-500/400` 硬编码 | P1 |
| `system/monitor` | 自绘卡片骨架 + 5 个子面板手写只读表 | P1 |
| `system/credential` | 手写 3 张只读表 + 卡片 | P1 |
| `system/export` | RePlusPage | 达标 |
| `system/setting` | RePlusPage | 达标 |
| `system/leave` | RePlusPage + addDialog | 达标 |
| `system/permission` | 抽屉 + 行内规则编辑 + 试算 | **基准**（RuleListEditor scss 104 行 → P2） |
| `system/account-risk` | RePlusPage + 处置表单内联在 TSX | P0 |
| `system/approval` | RePlusPage | 达标 |
| `system/approval/flow` | RePlusPage + RouteEditorForm / FlowNodesEditor | 达标 |
| `system/approval/instance` | RePlusPage + 审批动作表单内联在 TSX | P0 |
| `system/approval/rule` | RePlusPage + RuleForm | 达标 |
| `system/approval/delegation` | RePlusPage + 远程联想 | 达标 |
| `system/celery/task` / `crontab` / `interval` / `logs` | RePlusPage | 达标 |
| `system/config/system` / `config/user` | RePlusPage（+ addDialog） | 达标 |
| `system/logs/login` | RePlusPage | 达标 |
| `system/logs/operation` | RePlusPage（changes 列内联渲染 + 写死 `#f56c6c`） | P1 |

### 4.2 集成 / 设置 / 账号 / 用户 / 示例 / 聊天（15 页）

| 页面 | 实现形态 | 判定 |
|------|----------|------|
| `integration/ai` | 微信式两栏自绘 + 只读结果表 + 窄屏导航抽屉 | 例外 |
| `integration/ai/config` | RePlusPage + 档案表单；全局开关卡为卡片内联表单 | P1 |
| `integration/api-app` | RePlusPage + 行操作抽屉（ApiAppPanel） | 达标（面板 scss 85 行 → P2） |
| `integration/delivery` | RePlusPage（只读审计 + 行内重试） | 达标 |
| `integration/knowledge` | RePlusPage + 上传弹窗 + 管理抽屉 | 达标（弹窗自发起保存 → P2） |
| `integration/subscription` | RePlusPage + SubscriptionForm | 达标 |
| `settings/basic` / `settings/ldap` | SettingItem 通路（标签由后端下发） | 例外 |
| `settings/components/settings` | el-tabs 壳 | 例外 |
| `settings/login-policy` | RePlusPage + LoginPolicyForm | 达标 |
| `settings/message` | 手写 6 列表格 + 手写 el-dialog + 内联 el-form | P0 |
| `settings/security` | 拦截 IP 用 RePlusPage；Passkey 页签手写只读表 | P1 |
| `account` | 访问令牌 RePlusPage；调用日志手写表；51 行全局样式写死 `#fff` | P1 |
| `user/info` | 4 个独立表单面板 | 达标 |
| `user/notice` | RePlusPage + addDialog | 达标 |
| `demo/book` | RePlusPage + 元数据驱动表单 | 达标（示例建议词 hardcode → P2） |
| `chat` | 微信式两栏自绘（弹层已收敛） | 例外 |

### 4.3 数据分析 / 看板 / 表单 / 独立页（10 页）

| 页面 | 实现形态 | 判定 |
|------|----------|------|
| `analysis/report` | RePlusPage + ReportForm | 达标 |
| `analysis/screen` | RePlusPage + ScreenForm | 达标 |
| `analysis/screen/display` | 自绘投屏轮播 | 例外（`#9ca3af`、硬编码中文 → P1） |
| `dashboard` | 自绘栅格看板 | 例外（ChartCard 写死色值 → P1） |
| `dashboard/dataset` | RePlusPage + 只读预览弹窗 | 达标 |
| `form/designer` | RePlusPage + DynamicFormForm（字段子表） | 达标 |
| `form/my` | 手写表格列表（缺搜索/分页/列设置） | P0 |
| `welcome` | 自绘图表，4 个组件写死色板 | P0 |
| `about` / `empty` / `login` / `oauth/authorize` | 独立页面（自绘 / 登录壳） | 例外（about 的 scss 可收敛 → P2） |

## 五、重构清单

### 优先级 P0（体验割裂最明显）

| # | 页面 | 重构要点 |
|---|------|----------|
| 1 | `form/my`（我的填报） | 迁 `RePlusPage`：动态列用 `columnsFormat` 注入（`hideInTable` 控制），补搜索/分页/列设置；行操作分工统一为「填报/编辑（弹窗）+ 详情（抽屉）+ 审批轨迹」；保留 `SubmissionForm` / `SubmissionDetail` 内容组件 |
| 2 | `settings/message`（消息模板） | 抽 `components/MessageTemplateForm.vue` + `getPayload()`，编辑改走 `addDialog`（含预览按钮与结果区内联）；列表保留页签内轻量表格（注册表非分页资源，不适用 RePlusPage），补齐 testid 与空态口径 |
| 3 | `welcome`（工作台） | 4 个图表组件（Clock/Bar/Line/Round）与卡片色统一走主题（`epColor()` / EP 变量），补暗色模式验证 |
| 4 | `system/approval/instance` + `system/account-risk` | 审批/驳回/转办与风险处置表单抽独立 `components/*Form.vue` + `getPayload()`；行操作按钮收敛到 ≤5（6 → 5，低频项进「更多」） |

### 优先级 P1（一致性与主题正确性）

| # | 范围 | 重构要点 |
|---|------|----------|
| 5 | `system/credential`、`system/module` | 只读页统一为 RePlusPage 只读形态（关闭 create/update）或复用统一只读表；灰阶/尺寸改 EP 变量 |
| 6 | `account`、`settings/security`、`system/monitor` | 抽公共「只读明细表」组件，替换页签/面板内的手写表（表头、行高、空态与列表页对齐）；清理 `account/index.vue` 的 51 行全局样式 |
| 7 | `system/menu` | **不做整页迁移**（图标/权限点/拖拽交互重）；样式 token 化（去掉写死 32px/14px/4px），行高字号与列表页对齐；权限点预览表换统一只读表 |
| 8 | 硬编码颜色清理 | `logs/operation`(#f56c6c)、`user/useUserResetPassword`(五档红黄绿)、`tag/TagForm`(色板)、`dashboard/ChartCard`(#9a66e4)、`file/CategoryPieChart`、`monitor/HistoryChart`(#1d1e1f/#ffffff)、`screen/display`(#9ca3af)、`account`(#fff) |
| 9 | i18n 残留 | `analysis/screen/display.vue` 的「远程控制中」；`demo/book` 示例建议词（若需英文演示） |

### 优先级 P2（样式债与命名一致性）

| # | 范围 | 重构要点 |
|---|------|----------|
| 10 | 大块 scss 收敛 | 先替换写死值再减行数：`user/UserActionPanel`(165)、`chat/ChatEmojiPanel`(139，气泡场景可保留)、`permission/RuleListEditor`(104)、`api-app/*`(85/68/49)、`role/MenuPermissionTree`(60)、`knowledge/KnowledgePanel`(52)、`about`(24，含 `!important`) |
| 11 | 契约 / 命名 | `dashboard/CardForm` 的 `getCard` → `getPayload`；`integration/knowledge` 上传弹窗改为 `getPayload` 契约 |
| 12 | 形态小统一 | 两处窄屏导航抽屉（`chat`、`integration/ai`）抽公共组件；`login` 表单不拆分（形态等价，仅登记） |

## 六、批次与门禁

| 批次 | 范围 | 交付物 | 状态 |
|------|------|--------|------|
| 一 | P0-1 / P0-2：`form/my`、`settings/message` | 两页迁 RePlusPage + 表单抽离；各 1 条 E2E | **已交付（2026-09-24）** |
| 二 | P0-3 / P0-4：`welcome` 主题化、动作表单抽离 | 图表配色走主题；审批动作与风险处置表单独立化 | **已交付（2026-09-24）** |
| 三 | P1-5 / P1-6：只读表统一 | 抽公共只读明细表组件，替换 11 张手写表 / 8 个文件（含 `account` 全局样式清理） | **已交付（2026-09-24）** |
| 四 | P1-7~9：菜单页样式 token 化、颜色与 i18n 清理 | 主题/暗色下无写死色残留 | **已交付（2026-09-25）** |
| 五 | P2-10~12：样式与命名收敛 | scss 减行、契约命名统一 | **已交付（2026-09-25）** |

每批门禁：`typecheck` / `typecheck:strict` / eslint / prettier / stylelint / i18n / 行数 / vitest / 契约，
受影响页面跑 E2E 双浏览器，涉及核心页布局时复核 visual 基线。

## 七、不重构清单（合理例外登记）

| 页面 / 组件 | 保留理由 |
|-------------|----------|
| `chat`、`integration/ai` 主界面 | 微信式两栏 + 气泡是沟通形态的必然布局；弹层体系已收敛 |
| `dashboard`、`analysis/screen/display`、`welcome` 图表 | 栅格 / 画布 / 轮播布局；仅要求配色走主题（见 P0-3、P1-8） |
| `dashboard/dataset` 执行预览、`integration/api-app` 密钥明文、`system/permission` 的 `ScopeSelect` | 只读展示或**控件内选择弹窗**，非实体编辑弹层 |
| 表单内子表（审批节点 / 路由 / 规则 / 动态表单字段 / 填报明细） | 属于表单控件，不是列表页 |
| `settings/basic`、`settings/ldap` | 走 `SettingItem` 通路，字段标签由服务端 `search-columns` 下发 |
| `about`、`empty`、`login`、`oauth/authorize` | 独立页面（仅 `about` 的覆盖式样式建议在 P2 收敛） |

## 八、执行记录

### 批次一（2026-09-24）

**`form/my`（我的填报）**：列表迁 `RePlusPage`（新增 `utils/hook.tsx` 装配）。

- 顶部保留「可填表单」卡片区（填报入口，属选择表单语义，不进表格工具栏）；
- 提交数据（`data` JSON）不在后端 `table_fields` 中，由 `listColumnsFormat` 注入一列摘要（保持 `key: value` 形态，与详情抽屉及既有 E2E 断言同口径）；
- 状态列覆写为语义色 tag（字典色优先、缺省按 DRAFT/PENDING/APPROVED/REJECTED 兜底），无状态的提交显示「无需审批」而非留空；
- 个人页语义收敛：隐藏提交人列与对应搜索字段；
- 导出沿用**框架默认工具栏按钮**（`api.exportData` + 选中行/异步开关），不再自建导出入口；
- 行操作 5 个：详情 / 编辑（草稿态显示「继续编辑」）/ 提交 / 重新提交 / 删除；框架默认「编辑」「删除」按 `code` 关闭、默认「详情」用 `hideDetail` 关闭；
- 列表容器挂 `data-testid="my-submission-table"`，行内按钮与状态 tag 保留原 testid ⇒ 既有 `dform*.e2e.ts` **零改动**通过；
- 新增词条 `dform.form_name`（表单）、`dform.created_time`（提交时间）——RePlusPage 按 `dform.<字段名>` 取列标题。

**`settings/message`（消息模板）**：编辑弹窗由手写 `el-dialog` + 内联 `el-form` 改为 `addDialog` + 新组件
`MessageTemplateForm.vue`（`getPayload` 契约、内置预览按钮与预览结果区）；列表保留页签内轻量表格
（注册表非分页资源），补齐 `template-table` / `template-edit` / `template-reset` / `template-override-tag` 定位锚点。

**E2E**：新增 `e2e/form-my.e2e.ts`（卡片填报 → 框架列表：词条表头 / 分页 / 列设置 / 搜索 → 数据行 → 详情抽屉）
与 `e2e/message-template.e2e.ts`（编辑 → 预览 → 保存标记「已自定义」→ 重置回「代码默认」）；
回归 `dform.e2e.ts` / `dform-depth.e2e.ts` / `dform-flow.e2e.ts`，双浏览器全绿。

**验证**：typecheck / strict 全仓 / eslint / prettier / stylelint / i18n（zh/en 3001 对称）/ 行数 / vitest 464 /
build + 首屏闭包 546.9KB（预算内）；E2E 新增 2 条 + 既有 dform 3 条，chromium + webkit 均通过。

**经验（框架契约）**：

- 框架按钮（行操作与工具栏）**必须提供 `show`**：无 `show` 时 `Number(undefined)=NaN`，按钮被静默过滤；
- 自定义按钮的 `code` 不要与框架默认 `update` / `delete` / `detail` / `changeHistory` / `export` / `import` 冲突（按 `code` 去重，冲突时会顶掉默认按钮）；
- 列标题走 `localeName.<字段名>` 词条（覆写 `column.label` 在框架生成阶段会被 `formatPublicLabels` 结果优先，不生效）。

### 批次二（2026-09-24）

**`welcome`（工作台主题化，对应清单 #3）**

`src/utils/chartTheme.ts` 补齐主题取色能力（原有 `epColor()` 之外）：

- `epColorLight()`：读 EP 语义色的 `-light-9` tint 底色（暗色主题下 EP 会把它重定义为深色适配值，故深浅两态都跟随）；
- `cssVarColor()`：任意 CSS 变量 → 字面量（ECharts 不消费 `var()`，必须解析成色值）；
- `CHART_ACCENT`：图表专用强调色（`#9a66e4`，无 EP 语义对应）单点定义。

页面侧：

- 指标卡不再写字面色：`hook.tsx` 的 `bgColor` / `color` 改为 `tone: EpColorName`（请求数 = warning、用户数 = success、登录次数 = primary），渲染期经 `epColor()` / `epColorLight()` 取值；删掉原先的 `isDark ? "transparent" : bgColor` 暗色特判（EP 暗色已重定义 tint，无需再置透明）；
- 四个图表组件：`ChartLine`（默认色改为主题主色，线阴影随线色）、`ChartBar`（双色改 EP primary/success、渐隐尾端改主题底色）、`ChartRound`（轨道取 `--el-fill-color`、序列取主色）、`ChartClock`（数字取主色、小球色板取 5 个 EP 语义色，主题切换重建画布）；
- 同族色值顺带回源：`dashboard/ChartCard` 与 `monitor/HistoryChart` 的 `#9a66e4` 改用 `CHART_ACCENT`，两处导出图底色由写死深/浅色改 `cssVarColor("--el-bg-color-overlay")`（即清单 #8 的这两项，避免同名色值留在两处）。

**经验（ECharts 主题跟随）**：`@pureadmin/utils` 的 `useECharts` 在 `theme` 变化时会 `dispose()` 后用**最近一次 `setOptions` 的入参**重建实例——因此颜色必须在每次 `setOptions` 时解析，并 `watch(isDark, applyOptions)` 主动重设；只在 setup 里算一次颜色，切换主题后不会跟随（旧代码即如此）。

**`system/approval/instance` + `system/account-risk`（动作表单抽离，对应清单 #4）**

- 表单组件：`components/ApproveForm.vue` / `RejectForm.vue` / `UrgeForm.vue` / `AddSignForm.vue` / `TransferForm.vue`（审批实例）与 `components/RiskHandleForm.vue`（账号风险），统一 `getPayload()` 契约（**返回 `null` = 校验未过、调用方保持弹窗**）；单行与批量共用同一组件，只是弹窗标题与请求编排不同；
- 弹窗装配抽 `utils/instanceFormDialog.tsx::openActionDialog()`：统一 `destroyOnClose`（每次打开都是干净表单，无需 `closeCallBack` 手工复位）+ `sureBtnLoading` + **载荷为 `null` 时 `closeLoading()`**（ReDialog 不感知表单内部拦截，漏掉会让「保存」永久转圈）；批量动作拆到 `useInstanceBatchActions.tsx`；
- 行操作收敛：框架内置 icon 版「查看」与业务「申请详情」同 `code="detail"`，框架按 `code` 去重（后者胜出）；本次改为**显式 `hideDetail: true`** 以免依赖该去重细节，并显式 `showNumber: 5`（再多则按权重折叠进「更多」）。

**经验（按钮顺序权重）**：`OperationButtonsRow.show` 兼作**排序权重**——框架按 `show` 升序渲染，函数形态的返回值同样是权重。一律返回布尔会让所有条件按钮并列在最前（`Number(true) = 1`），需返回数字权重：通过 1 / 驳回 2 / 加签 3 / 转交 4 / 撤回 5 / 催办 6 / 重提 7 / 详情 9。

**E2E / 门禁**：`approval-flow` / `approval-transfer` / `approval-depth` / `approval-batch-export` / `approval-chain` / `approval-decision-info` / `approval` / `impact-preview` / `leave-apply` / `monitor` / `a11y` 回归；`visual` 6/6（welcome 配色变化在 2% 像素阈值内，无需重录）、`perf` 3/3（welcome CLS 0.0164、system-user 0.0213，无回归）。

### 批次三（2026-09-24）

**公共只读明细表 `ReReadonlyTable`（对应清单 #6 的组件部分）**

`src/components/ReReadonlyTable`：列声明 `ReadonlyColumn[]`（`prop` / `label` / `width` / `minWidth` / `align` / `showOverflowTooltip` / `fixed` / `slot`）+ `rows` / `loading` / `size` / `border` / `rowKey` / `maxHeight` / `emptyText`。统一此前各页面手写易漂移的三件事：

1. **表头**底纹与字色与列表页同源（`--el-table-row-hover-bg-color` / `--el-text-color-primary`）；
2. **空态**走表格 `#empty` 槽（不再「表格自带暂无数据 + 外层再挂一个 `el-empty`」的双空态）；
3. **尺寸**默认 `default`（与列表页行高对齐），卡片内嵌的密集面板显式传 `size="small"`（密度优先，调用处注明）。

替换 11 张手写表 / 8 个文件：`system/credential` 3 张、`system/module` 1 张（等级/状态/开关/覆盖走具名插槽）、`account/PatCallLogs` 1 张、`settings/security/PasskeyPanel` 1 张、`system/monitor` 5 张（服务/Redis/Celery 面板、慢请求、告警记录、任务健康、事件记录）。顺带补 `TaskHealthCard` 两张子表原本缺 `label` 的空表头。

**经验（组件透传）**：`ReReadonlyTable` 是单根（`el-table`），调用方的 `data-testid` 等 `$attrs` 自动落到表格容器，既有 `getByTestId("module-table")` 断言零改动。

**灰阶与页面级样式（对应清单 #5、#6 的样式部分）**

- `system/module`、`system/monitor/*`、`account/*` 的 `text-gray-400/500` 全部改 EP 文本色变量（`--el-text-color-regular` / `--el-text-color-secondary`）——Tailwind 固定灰阶在暗色下几乎不可读，是**主题正确性**问题而非风格偏好；
- `system/credential` / `system/module` 的只读形态：两页都是「卡片 + 多张只读表」而**非分页资源**，故按清单给的第二条路径办（复用统一只读表），不强行迁 `RePlusPage`；`module` 的开关列是配置编辑器语义，保留页面自有的保存/恢复工具栏；
- `account/index.vue`：原先 51 行、切成「非 scoped + 伪 scoped（`body[layout]` 全局规则）」两段的侧栏皮肤收敛为一段非 scoped 样式并补注释，写死的 `#fff` 改 `--el-color-white`（EP 未在暗色重定义该变量，行为等价）。

**E2E / 门禁**：受影响页面双浏览器 `approval-flow` / `approval-transfer` / `approval-depth` / `system-pages` / `monitor` / `passkey` / `mfa` 共 **57 passed**；另有 `pat` / `leave-apply` / `impact-preview` / `a11y` / `visual` / `perf` chromium 批次全绿。
门禁：typecheck、`typecheck:strict` 全仓、eslint、prettier、stylelint（`src`）、i18n（zh/en 3001 对称）、行数（0 超标）、contract、vitest 464、build + 首屏闭包 547.2 KB（+9.8 KB / 预算 15 KB）。

**环境提示**：`pnpm lint:stylelint` 的 glob（`**/*.{html,vue,css,scss}`）会扫到本地部署产物 `web/data/dist/`（未入库），本地会报 5 处历史告警；`stylelint "src/**"` 为 0，与本次改动无关。

**E2E 用例脆弱点（非本次回归，已登记 `xadmin-client/e2e/README.md`）**：`approval.e2e.ts` 的「待办角标归零」用例按**全库 PENDING** 计数，与其他审批 spec 同库连跑时会被前序 spec 遗留的待办顶住而失败（隔离/单 spec 跑通过）。

### 批次四（2026-09-25）

**`system/menu` 样式 token 化 + 权限点预览表统一（清单 #7）**

- 页面与四个子组件的写死值改 EP 令牌：树节点行高 `32px` → `var(--el-component-size)`、字号 `14px` → `var(--el-font-size-base)`、圆角 `4px` → `var(--el-border-radius-base)`；`MenuTreeRow` / `MenuTreeToolbar` / `MenuContextMenu` / `MenuPermissionDialog` 同步把 12px/13px 改 `--el-font-size-extra-small` / `-small`。取值与 EP 默认档完全一致（已核对 `theme-chalk` 定义），**零视觉差异**——`visual.e2e.ts` 的 `system-menu` 基线双浏览器无需重录即通过；
- 权限点预览表（`MenuPermissionDialog`）由手写 `el-table` + 外挂 `el-empty` 改为统一只读表 `ReReadonlyTable`：动作列走具名插槽、空态走组件内置 `#empty`（双动态文案保留）、`height="240"` 改 `:max-height="240"`；`menu-manage.e2e.ts` 的「将新建」断言零改动通过；
- 本页不迁 `RePlusPage`（图标/权限点/拖拽交互重），按清单口径只做 token 化与表格统一。

**硬编码颜色清理（清单 #8）**

- 逐项：`logs/operation` 慢请求标红 `#f56c6c` → `var(--el-color-danger)`；`screen/display` 空态文案色 `#9ca3af` → `var(--el-text-color-secondary)`；`file/CategoryPieChart` 环形留白边框 → `cssVarColor("--el-bg-color-overlay")`、兜底色板末位紫 `#9b59b6` → `CHART_ACCENT`；`tag/TagForm` 预设色板从 6 个十六进制改 `epColor()` + `CHART_ACCENT`（跟随自定义主色）；`dashboard/ChartCard`、`monitor/HistoryChart` 的 `#9a66e4` / `#1d1e1f` 已在批次二完成；
- 密码强度五档红黄绿在**三处重复**（`user/useUserResetPassword`、`user/info/UserInfoPassword`、`account/ChangePassword`），收敛为 `utils/passwordStrength.ts::passwordStrengthLevels(t)`：色值改 EP 语义色（danger → danger-light-3 → warning → success-light-3 → success），暗色主题下 `-light-3` 档由 EP 重定义，无需页面特判；
- 新增 `utils/tagTone.ts::SOLID_TAG_STYLE`（`border: none` + `var(--el-color-white)`），收敛 5 处「自定义色 tag 需补白字去边框」的内联字面量：`dict.ts` 的 `dictTagProps` / `statusTagProps`、`RePlusPage` 详情渲染器的 `dictTagStyle`、用户标签列 / 用户抽屉 / 标签列表色列 / 请假类型列；
- **顺带全站收敛固定灰阶文本色**（同属「暗色下不可读」，批次三已在 module / monitor / account 三处处理，本轮补齐其余页面）：30 个页面（53 处）+ 4 个公共组件 / 布局层文件（`ApiScopeEditor` / `ChangeHistoryDialog` / `ImportData` / 全局搜索弹层，7 处）共 60 处 `text-gray-400/500/600` → `text-(--el-text-color-secondary)` / `text-(--el-text-color-regular)`。两条例外判定：① `analysis/screen/display.vue` 的 `bg-gray-900/800`、`text-gray-300/400` 是该页**刻意的恒深色投屏配色**（不随主题），保留；② 带 `dark:` 变体的（`bg-gray-50 dark:bg-gray-800`、`text-gray-500! dark:text-white!`）本已主题自适配，不动。清理后全站仅剩两处灰阶：`ButtonOperation` 的操作列分隔（浅色灰 + `dark:text-white!`，自适配）、`SettingStretch` 的 `v-ripple` 波纹色（非文本色）。

**i18n 残留（清单 #9）**

- `analysis/screen/display.vue` 的「远程控制中」→ `t("dataScreen.remoteControlling")`（zh/en 词条已补，中文文案不变 ⇒ `analysis.e2e.ts` 的断言零改动）；
- `demo/book` 的出版社示例建议词原先在编辑表单与搜索区各写一份且为硬编码中文，抽为 `fetchPublisherSuggestions`（词条 `demoBook.publisherExample1~3`，zh/en 对称）——演示页在英文界面下不再出现中文建议词。

### 批次五（2026-09-25）

**大块 scss 收敛（清单 #10）**

- `user/UserActionPanel`（scss 163 行）**迁移到公共骨架 `ReActionPanel`**：资料卡走 `#profile` 插槽、基础信息走 `metaItems`、动作分组走 `groups`；本页动作契约带行参数（`run(row)` / `disabled(row)`），页面侧用 `panelGroups` 闭包绑定当前行后交给面板（`disabled` 绑行语义不变）。面板骨架（资料卡 / 两列信息 / 动作项 / warning·danger 语义色）与集成三页同源，**scss 163 → 37 行（文件 397 → 269 行）**；`data-action-code` 定位锚点未变，`user-panel.e2e.ts` 3 例双浏览器通过；
- `chat/ChatEmojiPanel`：popper 与分类条激活态的写死 rgba 阴影改 `var(--el-box-shadow)` / `var(--el-box-shadow-lighter)`，暗色特判块由 6 行收敛为 1 条规则（EP 暗色档会重定义阴影变量，无需页面再写一份）；
- 字号 / 圆角 token 化（取值等价、零视觉差异）：`12px→--el-font-size-extra-small`、`13px→--el-font-size-small`、`14px→--el-font-size-base`、`16px→--el-font-size-medium`、`20px→--el-font-size-extra-large`、`4px/2px → --el-border-radius-base/-small`；覆盖 `RuleListEditor`、`api-app/*`（3 个文件）、`MenuPermissionTree`（另把树节点行高 32px 改 `--el-component-size`）、`KnowledgePanel`、`about` 与菜单四件套；
- `about`：5 处 `!important` 经核查为**必需**（两张依赖卡的 `el-descriptions` 开了 `size="small"`，EP 的 `.el-descriptions--small … .el-descriptions__cell` 字号规则权重 0-4-0，压过页面任何常规选择器），本批只做字号 token 化 + 注释说明，不强行去除；
- 保留项（已核对）：`ApiGrantEditor` 里 `el-select` 的定宽（200 / 170 / 240 / 320px）**必须留在 scoped 类**——UnoCSS 的 `w-*`（权重 0-1-0）与 EP 的 `.el-select { width: var(--el-select-width) }`（0-1-0）同权重且实际被 EP 覆盖；这段是布局硬值而非主题值，无 EP 令牌对应。

**契约命名（清单 #11）**

- `dashboard/CardForm`：`getCard()` → `getPayload()`（对外 `defineExpose` 与 `useCardDialog.ts` 调用点同步），与全仓其余表单组件同契约；
- `integration/knowledge` 上传弹窗：删除自渲染的「取消 / 保存」按钮与自发起保存，改为**纯表单 + `getPayload()`**（校验未过返回 `null`，调用方保持弹窗）；提交、成功提示与列表刷新移到打开方 `beforeSure`（`sureBtnLoading` + 异常归一 + 「先 `done()` 关弹窗再刷新」），与其余表单弹窗完全同构。

**形态小统一（清单 #12）**

- 新增 `components/ReNavDrawer`（props：`narrow` 窄屏态、`width` 默认 `80%`；`v-model` 可见性；方向 `ltr`、无标题栏）：`chat` 与 `integration/ai` 两处逐字重复的窄屏导航抽屉收敛到同一外壳（各减 8 行），附 4 例单测（宽屏不渲染 / 窄屏参数与插槽 / 宽度透传 / 关闭回写 `v-model`）；
- `login` 表单按清单登记为**不拆分**：与注册 / 重置密码是同一形态的等价实现，拆组件只增加间接层。

**E2E / 门禁**：`knowledge` + `menu-manage` + `user-panel` 双浏览器 **18 passed**；`chat` / `ai` / `dashboard` / `analysis` / `api-app` 双浏览器 **36 passed**；`a11y` + `system-pages` + `role-permission` + `tag-center` + `file-center` + `leave-apply` + `operation-log` 双浏览器 **56 passed**；`visual` 12 passed（核心 6 页 × 2 浏览器，**无需重录基线**）；`perf` 3 passed（login CLS 0.0152 / welcome 0.0172 / system-user 0.0214）。
门禁：typecheck、`typecheck:strict` 全仓、eslint、prettier、stylelint（`src`）、i18n（zh/en 3005 对称）、行数（0 超标）、contract、vitest 473（+4 为新组件单测）、build + 首屏闭包 547.3 KB（+9.9 KB / 预算 15 KB）。

**经验（可复用）**

- **令牌替换要先核对取值**：`--el-font-size-extra-small/-small/-base/-medium/-extra-large` = 12/13/14/16/20px、`--el-border-radius-base/-small` = 4/2px、`--el-component-size` = 32px，与写死值逐一对齐后才替换，才能做到「零视觉差异 + visual 基线不重录」；`--el-component-size` 只在 `:root` 定义、组件不会局部重定义（已核对 `theme-chalk`）；
- **灰阶映射口径**：`text-gray-500/600` → `text-(--el-text-color-regular)`（#606266，对比度更高）、`text-gray-400` → `text-(--el-text-color-secondary)`；判例外先看有没有 `dark:` 变体（有则本就自适配）与页面是否刻意恒深色；
- **EP 组件上的 UnoCSS 宽高不生效**：`w-*` 与 EP 的组件根类同为 0-1-0 权重，实际由 EP 覆盖；`el-select` 这类需要定宽的场景只能用 scoped 类或行内样式（本批因此保留了 `ApiGrantEditor` 的定宽）；
- **`!important` 不一定是债**：`el-descriptions` 开 `size="small"` 时，EP 的 cell 字号规则权重 0-4-0，页面覆写只能 `!important`（about 页 5 处即此），清理前先算权重；
- **迁移到公共骨架时动作契约要转一次**：面板骨架用无参闭包（`run()` / `disabled()`），页面契约带行参数时在页面侧 `map` 绑定当前行即可，不要为单页反向修改公共组件；
- **python 批量替换源文件时注意行尾**：以 `$` 锚定且未把换行纳入捕获组的正则会把两行合并（本轮踩到并在 prettier / stylelint 门禁处暴露）；批量改完必须跑 `prettier --check` + `stylelint` 复核结构。

## 九、复核与查漏补缺（2026-09-25）

批次一~五交付后，按 §2.1 基准对全站做了一轮**机械复核**（脚本扫描 + 浏览器探针实测），
逐项核对 §五 的 12 项与 §四 的逐页判定，发现并修掉 5 处缺口；其余判定复核后维持。

### 9.1 复核方式与结论

见 §三 的「复核（2026-09-25）」表。要点：列表页形态、手写表格/弹窗、双空态（9 处均为
`v-if/v-else` 互斥分支）、写死颜色、i18n、行操作按钮数、表单契约、暗色模式验证九项逐条扫描。

### 9.2 本轮整改（5 处）

**① 写死字号 / 圆角补全令牌化（P2-10 收尾）**

批次五只覆盖了清单点名的 7 个文件，扫描发现页面与公共组件样式里仍有 **39 处 `font-size: Npx`
与 1 处 `border-radius: Npx`** 属可换令牌的取值。本轮按 §批次五同一口径（取值等价）全量替换，
涉及 17 个文件：`ReActionPanel`（面板骨架，被用户/集成四页共用）、`AiMessageBlock`、`AiThinking`、
`ReIcon/Select`、`RePlusPage/TagInput`、`integration/ai/AiProfilePanel`、`oauth/authorize`、
`approval/flow/FlowCanvas`、`system/TaskLogDialog`、`system/file/PreviewDrawer`、
`menu/MenuFormBasic`、`menu/MenuFormPermission`、`permission/RuleEditForm`、`permission/RuleFieldPicker`、
`permission/ScopeSelect`、`role/PermissionNodeLabel`、`role/PermissionTreeToolbar`。
`visual` 基线 12/12 未重录即通过（证明零视觉差异）。

**② 账户设置侧栏皮肤：删掉会外泄到全站的全局选择器**

批次三把「非 scoped + 伪 scoped」两段样式收敛为一段非 scoped 时，原伪 scoped（实为死代码）里的
`body[layout] .el-menu--vertical .is-active { color: #fff !important }` 被**激活成全局规则**。

- 实测（浏览器探针）：该选择器权重 `body[layout]`（元素+属性）+ 两个类 = **0-3-1**，
  高于同页 `.pure-account-settings-menu .el-menu-item.is-active`（0-3-0）——它其实是本页激活项白字的
  **实际来源**（只删该规则后实测字色由 `rgb(255,255,255)` 变 `rgba(0,0,0,.85)`）；
- 同时它会命中**主侧栏**的激活项（主侧栏菜单同样是 `el-menu--vertical`），
  当前因布局层规则权重更高而未生效（实测字色不变），属「迟早会突然生效」的隐患；
- 修法：保留规则但**锚定页面容器** → `body[layout] .pure-account-settings .el-menu--vertical .is-active`
  （0-4-1）。复验：账户页激活项 `rgb(255,255,255)` 且命中锚定规则；主侧栏 `rgba(0,0,0,.85)` 且不再命中。

**原理登记**：页面样式**不外泄**——即使跨组件（非 scoped）改 EP 内部样式，选择器也必须锚定页面容器；
无锚定的全局兜底选择器在权重账里非常危险（`body[layout]`/`[data-theme]` 这类「元素+属性」会额外加一分）。

**③ 表单契约命名对齐（P2-11 同类项）**

`defineExpose` 全量扫查（25 个 `*Form.vue`）后改名 2 处：

- `BatchUpdateForm.getFields()` → `getPayload()`（通用批量更新弹窗，调用点 `useBatchUpdate.tsx`）；
- `MenuDrawerForm.getModel()` → `getPayload()`（菜单抽屉表单，调用点 `useMenuDrawer.ts` 两处）。

其余保持独立命名的是**非弹窗载荷**语义：`RouteEditorForm.getRoutes()`、`RuleEditForm.validate()`
（行内编辑器）、`MenuDrawerForm` 的 `isDirty()` / `getCascadePks()` 等附加访问器。

**④ 暗色模式验证（补 P0-3 的交付物）**

P0-3 要求「补暗色模式验证」，批次二只做了浅色下的主题跟随核查，无自动化验证。本轮新增
`e2e/theme-dark.e2e.ts`（双浏览器 2 例）：

- 走顶栏「项目设置」面板的分段控件切换整体风格（**不能直接写 localStorage**：布局在挂载与 resize 时
  会把内存态回写存储，外部写入会被覆盖——实测「写存储 → 刷新」不生效）；
- 核心页在深色下必须是「深表面 + 浅文本」（`.el-card` / `.el-table` / `.pure-account-settings` 计算样式
  的亮度判据），切回浅色恢复；
- 账户设置侧栏激活项在两态下都是白字（守护 ② 的锚定修复）；
- **主题属服务端站点配置**（`WEB_SITE_CONFIG` 的 `DarkMode` / `ThemeMode`，改动即防抖 PATCH），
  用例开头归一浅色、结尾与 `afterEach` 双重还原，避免污染同库其它 spec 与视觉基线。

**⑤ 批量更新弹窗补端到端用例**

通用批量更新弹窗（角色/字典/部门等 5 页共用）此前无 UI 覆盖，与 ③ 的改名叠加后风险不可见。
新增 `e2e/batch-update.e2e.ts`：API 建临时角色 → 列表勾选 → 工具栏「批量更新」→ 选字段（白名单首项，
语言无关定位）→ 保存 → 断言接口 `is_active=false` 与列表开关回显（双浏览器通过）。

### 9.3 复核后确认不动的保留项

| 项 | 保留理由 |
|----|----------|
| 框架基础组件内的写死色（`ReQrcode` 白底、`ReSegmented` 阴影、`RePureTableBar` 边框、`ReIcon/Select` 阴影、`ReTypeit` 提示语） | 属框架层（模板继承实现），不在「页面级样式」基准内；改动风险大于收益 |
| `RePlusPage` 内的色板/状态色常量（`renderers-form.tsx` 取色板、`public.tsx` 开关与 tag 语义色，带 `dark:` 双态） | 是**数据**（色板）而非样式债，且已显式适配暗色 |
| `views/empty`、`oauth/authorize`、`login` | 独立页 / 模板页，§七 已登记 |
| `about` 的 5 处 `!important` | EP `size="small"` 的 cell 字号规则权重 0-4-0，页面覆写只能 `!important`（已在文件内注释说明） |
| `ApiGrantEditor` 的 `el-select` 定宽（200/170px） | UnoCSS `w-*`（0-1-0）与 EP `.el-select`（0-1-0）同权重且实际被覆盖；布局硬值无令牌对应 |
| `showNumber: 6` 的 5 个页面（`dict` / `leave` / `celery/task` / `mask` / `AccessToken`） | §2.1 的「≤5」是收敛口径而非硬约束；这 5 页是「框架内置按钮 + 页面动作同排」的显式取舍（页面内注释已写明），§四 逐页判定为达标 |
| 账户设置页 46 行非 scoped 侧栏皮肤 | 侧栏是「页签式导航」而非普通菜单（48px 行高、4px 圆角为有意设计），颜色全走 `--pure-theme-menu-*`，本轮只做锚定与注释 |
| 表单子表 / 只读明细 / 预览抽屉内的手写表（23 个文件） | §2.2 允许形态；双空态已核对为互斥分支 |

### 9.4 本轮门禁

- 静态：`typecheck`、`typecheck:strict` 全仓、eslint、prettier、stylelint（src）、i18n（zh/en 3005 对称）、
  行数（0 超标）、contract、vitest 473、build + 首屏闭包 547.3 KB（+9.9 KB / 预算 15 KB）；
- E2E（双浏览器）：`menu-manage` + `role-permission` + `permissions` + `system-pages` + `pat` + `mfa` **74 passed**；
  `oauth` + `passkey` + `api-app` + `ai` + `a11y` **37 passed**；`visual` 12 passed（无需重录）；
  `perf` 3 passed（login 0.0142 / welcome 0.0168 / system-user 0.0213）；
  新增 `theme-dark` 4 passed、`batch-update` 2 passed；
- 台账：`xadmin-client/e2e/README.md` 新增两条 spec 登记与「主题/站点配置类用例会污染同库其它 spec」陷阱节。
