# 年度回顾（2032.10 – 2033.09）

- 日期：2026-09-16（第七年度 12/12 窗口全交付）
- 关联：[年度开发计划-2032.10-2033.09.md](./年度开发计划-2032.10-2033.09.md)（窗口台账）、
  [阶段全景盘点-2027.10-2033.09.md](../阶段全景盘点-2027.10-2033.09.md)（跨年度）、
  [年度开发计划-2033.10-2034.09.md](../年度开发计划-2033.10-2034.09.md)（后继排期）、
  [ops/observability.md](../../ops/observability.md)、[ops/release-checklist.md](../../ops/release-checklist.md)

## 一、窗口完成度（12/12）

| 窗口 | 主项 | 产出 |
|------|------|------|
| 2032-10 | 运营基线核对 | checklist 核对；SLO 快照复测；指标端点与容器健康 |
| 2032-11 | **清账窗口** | **五仓库全部推送**（server 19 / client 5 / docs 7 / installer 2 / web 1）；**发现并修复 regen 门禁**——`search-columns.d.ts` 缺 `suggest_url`（后端合约已支持、生成物未同步）→ 本地复现 → 同步提交；dispatch 404 口径确认（合 main 后执行）；CSP 头随 web 推 main |
| 2032-12 | SLO 校准 + 演练 | T6 维持等待；**演练第十轮（SECRET_KEY 轮换）**——JWT 三态闭环（200 → 轮换后 401 → 恢复后 200）+ 重启期 celery 心跳续期观察 |
| 2033-01 | 依赖窗口 | 季度 audit **双 0** |
| 2033-02 | 按需插槽一 | 无新触发 |
| 2033-03 | 交付工程 + 演练 | **演练第十一轮（敏感泄漏扫描）**——值级零泄漏（SECRET_KEY / METRICS token / JWT）；config.yml gitignore 确认；`password` 掩码机制验证；`tmp_token` 登记 |
| 2033-04 | 按需插槽二 | 无新触发 |
| 2033-05 | 前端跟随 | 仅 2 项 major（维持）；**CI 全绿复核**（regen 门禁 + Lint + E2E） |
| 2033-06 | 审计与安全 + 演练 | **演练第十二轮（敏感权限审计 + 加固）**——`config.yml` 644 → **加固 600**；`data` 目录收紧登记；installer 侧权限核对登记 |
| 2033-07 | 滚动债与评估出口 | 债务 **0**；评估出口 **+3**（`tmp_token` / DEBUG 正文裁剪 / `convert_type` 沿用） |
| 2033-08 | 缓冲与核对 | checklist 挂起项核对；资源盘点；盘点素材汇总 |
| 2033-09 | 年度回顾 + 规划 + 盘点 | 本篇 + [跨年度全景盘点](../阶段全景盘点-2027.10-2033.09.md)（六年度 / 演练 12 轮 / KPI 纵览）+ 第八年度计划；索引同步 |

## 二、年度主线：清账执行年

第六年度把「清账」与「SLO 校准」转入就绪态；**本年度完成清账首轮执行**：

| 项 | 结果 |
|----|------|
| 五仓库推送 | server 19 · client 5（含修复）· docs 7 · installer 2 · web 1 —— **34 个提交到位**，远端 5/5 同步 |
| **regen 门禁修复** | CI 三轮 Lint 失败根因定位：**元数据契约类型未同步**（后端 `suggest_url` 合约已支持、`search-columns.d.ts` 未重生）→ 本地 `pnpm gen:metadata-types` 复现同 diff → 同步提交（含 rebase 远端自动提交）→ **CI 全绿** |
| 清账剩余 | 视觉基线 / Renovate dispatch（**合 main 后**，404 口径已确认）；CSP 页面层（部署形态） |
| SLO 校准 | 维持就绪（数据口径未达 3 个月） |

## 三、安全演练三连（第十 ~ 十二轮，累计 12 轮）

| 轮次 | 场景 | 关键结论 |
|------|------|----------|
| 第十轮 | **SECRET_KEY 轮换** | JWT 三态闭环（旧 key **200** → 轮换后 **401** → 恢复后 **200**）；轮换 = 全端重登影响面确认；重启期 celery 心跳续期（探针语义正确） |
| 第十一轮 | **敏感泄漏扫描** | SECRET_KEY / METRICS token / JWT **值级零泄漏**；`config.yml` 已 gitignore；`password` 掩码机制在工作（`*` 串）；`tmp_token` 完整记录登记（短时效） |
| 第十二轮 | **敏感权限审计 + 加固** | 发现 `config.yml` 644（密钥文件全局可读）→ **加固 600**（服务无影响）；`data` 755 登记；installer 侧核对登记 |

## 四、KPI 与门禁

| 指标 | 第六年度末 | 第七年度末 | 说明 |
|------|-----------|-----------|------|
| 后端测试 | 2395 | 2395 | 本年度无后端变更 |
| 前端 | 250 | 250 | +1 类型同步修复提交；CI 全绿 |
| 依赖安全 | 双 0 | **双 0** | 两轮复核 |
| 代码债务 | 0 | **0** | 全仓 TODO/FIXME |
| 演练轮次 | 9 | **12** | +3（密钥 / 泄漏 / 权限） |
| 清账 | 就绪 | **首轮执行**（推送 + CI 修复） | 剩余项挂合 main |

## 五、遗留与开放项（交接清单）

| 项 | 阻塞条件 | 重开动作 |
|----|----------|----------|
| 视觉基线 + Renovate 验收 | 合 main | `gh workflow run`（404 口径已验证） |
| CSP 页面层切强制 | 部署形态 | 违规观察零 → 去 `Report-Only` |
| SLO 校准（T6） | 数据 ≥3 个月 | 快照工具 + 回填 |
| 异地副本 | 独立盘 | `BACKUP_REMOTE_TYPE` |
| Django 6.2 / TS 7 / cropperjs 2 / docs 18 | 发布与窗口 | ADR-004 / ADR-014 / overrides |
| 评估出口（+3）：`convert_type` / DEBUG 正文裁剪 / `tmp_token` 时效 | 各自条件 | 触发即插 |
| 演练剩余场景池 | — | 磁盘压力 / 资源限制 / DNS 复演 |

## 六、第八年度方向（详见年度开发计划-2033.10-2034.09）

延续**持续运营年**：清账闭环（合 main dispatch）/ SLO 校准（数据成熟）/ 演练轮换（剩余池），
跨年度视角见 [阶段全景盘点](../阶段全景盘点-2027.10-2033.09.md)。
