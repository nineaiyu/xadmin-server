# docker compose 凭据缓存（.env 已被 gitignore，勿提交、勿手工编辑）。
#
# 本文件是 utils/compose_env.sh 维护的「派生缓存」，不是第二个配置文件：
# dev_up.sh / dev_down.sh 每次运行都会以 config.yml 的同名键为准自动同步本文件。
# 你只需要编辑 config.yml（DB_PASSWORD / REDIS_PASSWORD 写在同名键）。
#
# 为什么它必须存在：compose 的变量插值读不了 YAML，只能读环境变量 / .env
# （KEY=VALUE 格式）；有了这份自动维护的缓存，绕过脚本直接操作
# `docker compose <任意命令>`（restart / logs / down 等）也都可用。
# 应用容器（server / celery×3）会透传这两个变量，但仅当 config.yml 未定义同名键时生效。
DB_PASSWORD=
REDIS_PASSWORD=

# 可选：构建与运行参数（不填用内置默认值）
# PIP_MIRROR=https://mirrors.cloud.tencent.com/pypi/simple
# APT_MIRROR=http://mirrors.aliyun.com
# UV_CONCURRENT_DOWNLOADS=50
# TZ=Asia/Shanghai
# DB_DATABASE=xadmin
# DB_USER=server
# REDIS_PORT=6379
# VOLUME_DIR=../
